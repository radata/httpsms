.env.local
*main.exe
tmp
$path

.env*
.flaskenv*
!.env.project
!.env.vault
!.env.docker
cmd/experiments/*
.DS_Store

# Kernel core dumps (created on crash, can be hundreds of MB).
# The dev container now runs with `ulimit -c 0`, so these should stop
# appearing - the pattern stays for anything already on disk elsewhere.
core
core.[0-9]*