*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
.kotlin/sessions/
local.properties

# Release signing. keystore.properties holds the keystore password in the
# clear and *.jks is the signing key itself — neither ever goes to git.
/keystore.properties
*.jks
*.keystore
.DS_Store

# Kernel core dumps (created on crash, can be hundreds of MB).
# The dev container now runs with `ulimit -c 0`, so these should stop
# appearing - the pattern stays for anything already on disk elsewhere.
core
core.[0-9]*